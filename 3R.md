语义标签本质上是把三种本来各自独立生成的产物（源码、program.json、merged_swimlane.json）连接起来的「外键」。下面这张图是整套联动的骨架——三栏共享同一条 label_id 主线，点击任一栏即可点亮其余两栏：VUnable to reach visualize整套算法分四部分：先定义一个能跨三栏对齐的标签模型，再讲标签从哪来（含框架自动解析），然后讲标签如何在编译链路里「不掉队」，最后讲加载和交互时怎么联动高亮。
一、统一标签模型：用 label_id 做连接键
最容易踩的坑是「三份文件各自独立生成，运行时再靠 label 字符串去匹配」。这种做法很脆——同名标签（比如循环里多次出现的 softmax）会互相串台，融合后的算子又可能丢失原始字符串。所以这套算法的第一原则是：以溯源传播（provenance）为主、字符串匹配为辅。标签在编译期就以稳定 ID 的形式焊进每个产物，运行时直接查 ID，字符串只在跨文件、跨次运行导致溯源断裂时兜底。
为此给每个被标记的元素挂两把键：

label_key：规范化的语义名（如 model/encoder/layer3/attention/softmax），用于「同名跨出现聚合」——这正是锁定模式下「下拉选出所有相同 semantic_label 子图」要用的键。
instance_id：单次出现的稳定 ID（按调用点/作用域路径哈希得到），用于悬停时的 1:1 精确高亮，区分同一个 softmax 标签在不同层的不同实例。

悬停用 instance_id，锁定用 label_key，两套粒度分工明确。
二、语义标签的获取（显式 + 框架自动解析）
显式写入 的 set_semantic_label("softmax") 语义上是「设置当前标签上下文」：它把一个叶子标签压进一个线程局部的上下文栈，其后创建的所有算子都继承这个叶子名，直到下一次调用或作用域退出。这给开发者最高优先级的命名权。
框架自动解析 用四条互补策略，让没写显式标签的代码也不留空白：

运行期调用栈捕获（最可靠）：PyPTO 在 tracing 构图时，每创建一个算子就用 inspect/sys._getframe 抓调用栈，向上找到第一个用户代码帧——它就是源码行（如 lineno: 69）；继续向上的用户函数构成作用域路径；算子分发名（softmax、matmul）作为候选叶子名。这跟 TorchFX/Dynamo 给节点记 stack_trace 是同一套思路，能跟着真实执行走，天然处理动态控制流。
静态 AST 作用域：ast.parse 源文件，从 FunctionDef/ClassDef/with 块构建作用域树，得到「行号 → 作用域路径」和精确的 lineno/end_lineno。这一步专门为代码色带提供连续行区间，也给纯数据流代码补结构。
计算图模式匹配：对图跑模式匹配器，识别 Sub→Exp→ReduceSum→Div（softmax）、LayerNorm、matmul→scale→softmax→matmul（attention）等融合模式，给匹配到的子图合成一个标签。这处理「用户用底层原语手写、根本没有单次 API 调用」的情况——通常可直接复用框架已有的融合规则引擎。
变量名启发（弱信号）：从 AST 取赋值左值（softmax_out、attn_weights）作为显示名补充，低置信度，仅在前三者都给不出好名字时用来美化标签。

合并规则：优先级为 显式 > 调用栈 > 模式匹配 > AST 作用域 > 变量名。但关键不是「谁覆盖谁」，而是分工合成——显式（或算子 API 名）提供叶子语义名，调用栈/AST 提供外层作用域路径，二者拼成层级标签。例如开发者写了 set_semantic_label("softmax") 而这段又在 def attention() 里，最终 label_key 就是 .../attention/softmax。只有当显式名与自动推断的叶子名冲突时（用户把一个 matmul 标成 qk_score），显式名胜出做叶子。每个标签同时记 source 字段（explicit / auto_trace / auto_ast / auto_pattern）和置信度，方便前端区分展示与调试。
三、标签随编译链路传播（联动鲁棒性的关键）
这是整套设计的核心机制。编译链路每做一次下降/变换，都把来源节点的标签拷贝并合并到产物上，维持一条 源码行 →(产生) 图节点 →(调度) Task 的溯源链。算子融合时取并集（一个融合 Task 可能同时关联多个 label_key），下降时直接继承。类比 LLVM 里 !dbg 调试元数据随 IR 传递——联动索引由此可以「沿溯源链构建」，而不是「重新字符串匹配」。
落到产物上，给 program.json 节点的 semantic_label 扩展两个字段：
json"semantic_label": {
  "label_key": "model/encoder/layer3/attention/softmax",
  "instance_id": "n_1f3a9c",
  "name": "softmax",
  "source": "explicit",
  "scope": ["model", "encoder", "layer3", "attention"],
  "filename": "D:\\code\\...\\test_softmax_custom.py",
  "lineno": 69, "end_lineno": 69, "col_offset": 4
}
泳道图 Task 因为可能由融合算子产生，标签是个集合：
json{ "task_id": "task_128",
  "label_keys": ["model/.../attention/softmax"],
  "instance_ids": ["n_1f3a9c", "n_1f3aa0"] }
name/filename/lineno 与现有结构保持兼容，新增的 label_key/instance_id 是真正驱动联动的字段；旧字段在溯源断裂时仍可作为字符串兜底键。
四、加载与交互：构建索引 + 联动高亮
加载期（打开三栏时一次性完成）。先做路径归一化解决文档里那条「色带不显示」的坑：把 filename 统一成相对工作区根的规范绝对路径，归一斜杠（Windows \\ vs /），Windows 下大小写折叠；万一路径仍对不上，用源文件内容哈希做最后兜底匹配。然后构建双向索引：
LabelIndex[label_key]   → { code_ranges[], graph_nodes[], swimlane_tasks[] }   // 聚合，供锁定
InstanceIndex[instance_id] → { code_range, graph_nodes[], swimlane_tasks[] }   // 精确，供悬停
ReverseIndex[元素]       → [label_key / instance_id]                            // 任一栏反查
构建时优先按 instance_id/label_key 分组；缺这些字段的元素退化为按 (filename, lineno) 匹配，再退化为按 label 名匹配。代码色带把相邻同顶层标签的行合并成连续区间，配色用 color = hash(label_key) 这种确定性映射，保证三栏对同一标签用同一种颜色。
交互期。悬停/移动光标时：
onHover(view, elem):
    labels = ReverseIndex[elem]                 // 嵌套作用域可能命中多个
    active = mostSpecific(labels)               // 取最深 scope 作主高亮
    for v in 其余两栏:
        highlight(v, InstanceIndex[active.instance_id][v], 强)
        highlight(v, ancestorsOf(active)[v],          弱)   // 祖先作面包屑、降透明度

onLock(elem):
    pin = labelKeyOf(elem)
    graphDropdown = subgraphsContaining(pin)    // 列出所有含该 label_key 的子图，支持跨子图导航
    维持按 pin 的高亮直到解锁
主高亮取最深作用域（最具体的那层），祖先标签以低透明度作面包屑，这样嵌套结构（attention 里套 softmax）既能看到精确命中、又能看到上下文。一个融合 Task 关联多个标签时全部点亮，主标签取离光标最近的那个。锁定就是把 active 冻结成 pin，并激活计算图左上角的跨子图下拉；解锁回到悬停跟随模式。
边界处理几条值得单列：被死代码消除（DCE）掉的标签在图里没有对应节点，应显示「无映射」态而不是空高亮；没写显式标签的代码靠第二节的自动标签兜底，不会出现「整段无色带」；跨文件、跨次运行导致 instance_id 对不上时，按前述退化链降级。性能上索引预构建是 O(n)、交互查表是 O(1)，几千节点规模下悬停联动可做到无感延迟。

这套设计的取舍核心是：把联动的可靠性建立在编译期的标签溯源上，而不是运行期的字符串撞名上；自动解析则用「调用栈为主、AST/图模式/变量名为辅」的组合，保证即便开发者一个 set_semantic_label 都没写，框架也能给出一套结构化、可联动的标签。如果你想，我可以把第二节的自动解析或第三节的传播过程再单独画一张流程图细化。
